<%# app/views/lists/index.html.erb %>
<div class="container mt-4">
  <h1>Mes Listes de Films</h1>
  <ul>
    <% @lists.each do |list| %>
      <li><%= link_to list.name, list_path(list) %></li>
    <% end %>
  </ul>
  <%= link_to "Créer une nouvelle liste", new_list_path, class: "btn btn-primary mt-3" %>
</div>
